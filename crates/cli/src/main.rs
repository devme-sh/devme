//! `devstack` — the user-facing CLI. Dispatches to the TUI for foreground
//! mode, to subcommands (`up`, `down`, `status`, `logs`, `errors`, `ps`,
//! `restart`, `health`, `overrides`, `instances`, `completions`,
//! `agent-context`) for everything else.
//!
//! See `docs/adr/0008-cli-tui-conventions.md`.

fn main() {
    eprintln!("devstack: not yet implemented");
    std::process::exit(1);
}
