//! Per-repo shared-services daemon. Owns `repo`-scoped services.
//! See `docs/adr/0007-two-tier-daemon.md`.

fn main() {
    eprintln!("devstack-shared-supervisor: not yet implemented");
    std::process::exit(1);
}
